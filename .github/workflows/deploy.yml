name: Deploy (main)

on:
  push:
    branches: [main]

concurrency:
  group: deploy-main
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest

    environment:
      name: Production
      url: https://forms-doc.os.mieweb.org/

    steps:
      - name: Deploy over SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          port: ${{ secrets.DEPLOY_PORT }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_KEY }}
          script: |
            set -e
            cd "${{ secrets.DEPLOY_DIR }}"
            git fetch --all
            git reset --hard origin/main
            npm ci
            npm run build
            pm2 reload forms --update-env
            pm2 save

      - name: Add deployment links
        run: |
          echo "## 🚀 Production Deployment" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- 📘 Docs: https://forms-doc.os.mieweb.org/" >> $GITHUB_STEP_SUMMARY
          echo "- 🧪 Demo: https://forms-demo.os.mieweb.org/" >> $GITHUB_STEP_SUMMARY
