// Removed: forms-editor is now a library package. This bootstrap was only for the old standalone build.
// If you need an example usage, create one in an application (e.g., apps/web-editor) importing QuestionnaireEditor.
export {}
